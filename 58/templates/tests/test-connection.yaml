apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "58.fullname" . }}-test-connection"
  labels:
    {{- include "58.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "58.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
