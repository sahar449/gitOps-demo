apiVersion: v1
kind: Service
metadata:
  name: {{ include "58.fullname" . }}
  labels:
    {{- include "58.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "58.selectorLabels" . | nindent 4 }}
